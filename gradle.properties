org.gradle.jvmargs=-Xmx1G
# No org.gradle.java.home pin here on purpose — it's a machine-specific
# absolute path (was pointing at a JDK 26 install neither machine's Gradle
# 8.6/Loom 1.6 combo can run on). Set JAVA_HOME to a JDK 17-21 install before
# running ./gradlew instead.
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.15.11
fabric_version=0.92.2+1.20.1
mod_version=1.10.4
maven_group=com.prizmo
archives_base_name=solsai
